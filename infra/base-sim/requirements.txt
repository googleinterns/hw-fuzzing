cocotb==1.4.0
Mako==1.1.3
PTable==0.9.2  # for seed composer (TODO: move to package)
PyYAML==5.3.1  # for seed composer (TODO: move to package)

# Development version with OT-specific changes
git+https://github.com/lowRISC/fusesoc.git@ot#egg=fusesoc >= 1.11.0

# Development version with OT-specific changes
git+https://github.com/lowRISC/edalize.git@ot#egg=edalize >= 0.2.0
